const Die = ({value}) => {
    return (
        <button className="bg-white h-9 w-9 font-bold text-xl rounded-sm shadow-xl cursor-pointer">
            {value}
        </button>
    )
}

export default Die